{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/api.js" }
  ],
  "buildCommand": "cd spotifymap-frontend && npm install && npm run build",
  "outputDirectory": "spotifymap-frontend/dist",
  "framework": "vite"
}